{
  "name": "yauhenm-blog-builder",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Builds blog HTML pages from blog-source/posts/*.md into v4-deploy/blog/",
  "scripts": {
    "build": "node build-blog.mjs",
    "build:watch": "node --watch build-blog.mjs"
  },
  "dependencies": {
    "marked": "^18.0.3",
    "gray-matter": "^4.0.3"
  }
}
